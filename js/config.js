// Public client-side config. Server-side secrets (ACS connection string, etc.)
// live in the SWA app settings — never in committed JS.
const CONFIG = {
  CONTACT_EMAIL: "sales@neomeric.com",
  CONTACT_NAME: "Neomeric AI",
};
